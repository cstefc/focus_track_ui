describe("Projects Screen", () => {
    it("reflects project updates in the UI", async () => {
        // GIVEN

        // WHEN

        // THEN

    })

    it("shows an empty state when no projects exist", async () => {
        // GIVEN

        // WHEN

        // THEN

    })

    it("navigates to project details when clicking a card", async () => {
        // GIVEN

        // WHEN

        // THEN

    })
})
