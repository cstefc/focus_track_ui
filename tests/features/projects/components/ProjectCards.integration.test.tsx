describe('ProjectCards', () => {
    it("renders correctly", async () => {
        // GIVEN

        // WHEN

        // THEN

    })

    it("loads projects", () => {
        // GIVEN

        // WHEN

        // THEN

    })

    it("shows loaded projects", () => {
        // GIVEN

        // WHEN

        // THEN

    })

    it("shows text when no projects exists", async () => {
        // GIVEN

        // WHEN

        // THEN

    })

    it("shows a spinner when loading", () => {
        // GIVEN

        // WHEN

        // THEN

    })

});